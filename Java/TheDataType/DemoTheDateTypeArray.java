package Summarize.TheDataType;

public class DemoTheDateTypeArray {
    /*数据类型-数组
      查询快：数组的地址是连续的，我们通过数组的地址可以找到数组，通过索引可以快速查找某个元素
      增删慢：数组的长度是固定的，我们增删一个元素，必须创建一个新数组，把源数组的元素复制过来
      要把数组中索引是3的元素删除，必须创建一个系的数组，长度是源数组的长度-1，吧源数组的其他元素
      复制到数组中，再把新数组的地址值赋值给变量，源数组会在内存中销毁（垃圾回收）
      在栈内存中，频繁的创建数组，复制数组中的元素，销毁数组，效率低下*/
}
