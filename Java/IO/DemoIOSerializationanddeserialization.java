package Summarize.IO;

public class DemoIOSerializationanddeserialization {
    /*序列化和反序列化
    把对象从流的方式写入到文件中保存，叫写对象，也角度想的序列化
    对象中包含的不仅仅是字符，使用字节流
    ObjectOutputStream ：对象的序列化流  写writerObject（）；
    把文件中保存的对象，以流的方式读取出来，叫做读对象，也叫做对象的反序列化流
    读取的文件中保存的都是字节，使用字节流
    ObjectInputStream：对象的反序列化流 读readObject（）；
    */
}
