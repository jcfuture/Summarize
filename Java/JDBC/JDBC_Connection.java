package Summarize.JDBC;

public class JDBC_Connection {
    /*Connection 数据库连接对象
    *   功能：
    *       获取执行sql的对象 Statement createStatement() 创建一个对象，用于将 SQL 语句发送到数据库。Statement
    *                      PreparedStatement prepareStatement(String sql) 创建一个对象，用于将参数化的 SQL 语句发送到数据库。PreparedStatement
    *   管理事务：
    *       开启事务：void setAutoCommit(boolean autoCommit) 将此连接的自动提交模式设置为给定状态。
    *       提交事务：void commit() 使自上次提交/回滚以来进行的所有更改永久化，并释放此对象当前持有的任何数据库锁。Connection
    *       回滚事务：void rollback() 撤消当前事务中进行的所有更改，并释放此对象当前持有的任何数据库锁。Connection*/
}
